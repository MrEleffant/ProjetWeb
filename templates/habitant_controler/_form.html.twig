{{ form_start(form, {'attr': {'class': 'mt-4'}}) }}
    {{ form_widget(form) }}
<button type="submit" class="mt-4 inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
