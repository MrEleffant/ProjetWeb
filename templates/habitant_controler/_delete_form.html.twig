<form method="post" action="{{ path('app_habitant_controler_delete', {'id': habitant.id}) }}" onsubmit="return confirm('Etes vous sure ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ habitant.id) }}">
    <button class="mt-4 inline-block px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600">Delete</button>
</form>
